#include<stdlib.h>
* main - Determines if a number is positive, negative or zero.
* Return: Always 0 (success)
*/
int main(void)
{
int n:
srand(time(0));
n = rand() - RAND_MAX / 2;
if (n > 0)
(
printf(d is poitive\n".n):
)
else
{
printf(" % d is negative\n",n):
}
return (0):
